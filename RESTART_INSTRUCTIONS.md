# 🔄 تعليمات إعادة التشغيل الصحيحة

## ⚠️ مهم جداً!

التغييرات التي تمت **لن تظهر** مع Hot Reload العادي (`r`)

يجب عمل **Hot Restart** أو إعادة تشغيل كاملة!

---

## ✅ الطريقة الصحيحة

### الطريقة 1️⃣: Hot Restart (الأسرع)
في Terminal، اضغط:
```
R
```
**ملاحظة:** حرف **R كبير** وليس `r` صغير!

---

### الطريقة 2️⃣: إعادة تشغيل كاملة (الأضمن)

#### الخطوة 1: أوقف التطبيق
في Terminal، اضغط:
```
Ctrl+C
```

#### الخطوة 2: أعد التشغيل
```bash
flutter run
```

---

## 🔍 كيف تعرفين أن التغيير تم؟

بعد إعادة التشغيل، افتحي شاشة "إضافة وجبة":
- ✅ يجب أن **لا ترى** الأيقونات (الإيموجي)
- ✅ يجب أن ترى **الأسماء فقط** بخط أكبر

---

## 🆘 إذا لم يعمل

جربي هذه الخطوات:

### 1. Clean & Rebuild
```bash
flutter clean
flutter pub get
flutter run
```

### 2. تحقق من الملفات
تأكدي أن التغييرات موجودة في:
- `lib/presentation/screens/composite_meal_builder_screen.dart`
- `lib/presentation/screens/composite_meals_screen.dart`

---

**ملاحظة:** Hot Reload (`r`) لا يكفي للتغييرات الكبيرة!
يجب عمل Hot Restart (`R`) أو إعادة تشغيل كاملة.
