# دليل الإشعارات الدورية ⏰

## التحديث الجديد: إشعارات كل 3 دقائق! 🔔

تم إضافة ميزة الإشعارات الدورية التي ترسل تذكيرات تحفيزية كل 3 دقائق لتشجيع المستخدمين على تسجيل وجباتهم.

---

## 📋 الملفات المعدلة:

### 1. **notification_service.dart**
**المسار**: `lib/data/services/notification_service.dart`

#### ✨ الإضافات الجديدة:

```dart
// متغيرات جديدة
Timer? _periodicTimer;
static const int _notificationIntervalMinutes = 3;
```

#### 🔧 الدوال الجديدة:

1. **`startPeriodicNotifications()`**
   - تبدأ الإشعارات الدورية كل 3 دقائق
   - تعرض أول إشعار فوراً
   - تحفظ الحالة في SharedPreferences

2. **`stopPeriodicNotifications()`**
   - توقف الإشعارات الدورية
   - تلغي المؤقت
   - تحدث الحالة المحفوظة

3. **`_showPeriodicNotification()`**
   - تعرض إشعار تحفيزي عشوائي
   - تحتوي على 10 رسائل مختلفة
   - تحفظ في سجل الإشعارات

4. **`isPeriodicNotificationsEnabled()`**
   - تتحقق من حالة الإشعارات الدورية

5. **`restorePeriodicNotifications()`**
   - تستعيد الإشعارات عند إعادة فتح التطبيق

#### 💬 الرسائل التحفيزية:

```dart
'💪 حان وقت تسجيل وجبتك!'
'🍎 لا تنسى تناول وجبة صحية'
'⏰ تذكير: سجل وجبتك الآن'
'🥗 حافظ على نظامك الغذائي'
'📊 سجل وجبتك لتتبع تقدمك'
'🎯 أنت على الطريق الصحيح!'
'💚 وجبة صحية = جسم صحي'
'⭐ استمر في التقدم!'
'🔥 حقق أهدافك الغذائية'
'✨ كل وجبة مهمة!'
```

---

### 2. **settings_cubit.dart**
**المسار**: `lib/logic/cubits/settings_cubit.dart`

#### ✨ التحديثات:

```dart
// إضافة حالة جديدة
class SettingsState {
  final bool periodicNotificationsEnabled;
  // ...
}

// دالة جديدة
Future<void> togglePeriodicNotifications() async {
  if (newValue) {
    await NotificationService().startPeriodicNotifications();
  } else {
    NotificationService().stopPeriodicNotifications();
  }
}
```

---

### 3. **settings_screen.dart**
**المسار**: `lib/presentation/screens/settings_screen.dart`

#### ✨ واجهة جديدة:

تم إضافة قسم جديد في الإعدادات:

```
┌─────────────────────────────┐
│ 🔔 الإشعارات               │
├─────────────────────────────┤
│ 🔔 الإشعارات مفعلة         │
│    ستتلقى إشعارات...       │
│                      [ON]   │
├─────────────────────────────┤
│ ⏰ تذكير دوري              │
│    تذكير كل 3 دقائق        │
│                      [OFF]  │
└─────────────────────────────┘
```

**المميزات:**
- زر تبديل (Switch) لتفعيل/إيقاف
- يتعطل تلقائياً إذا تم كتم الإشعارات
- أيقونة ⏰ للتذكير الدوري
- نص توضيحي "تذكير كل 3 دقائق"

---

### 4. **main.dart**
**المسار**: `lib/main.dart`

#### ✨ التحديث:

```dart
// استعادة الإشعارات الدورية عند فتح التطبيق
await NotificationService().restorePeriodicNotifications();
```

---

## 🎯 كيفية الاستخدام:

### للمستخدم:

1. **افتح التطبيق**
2. **اذهب إلى الإعدادات** ⚙️
3. **ابحث عن قسم "الإشعارات"** 🔔
4. **فعّل "تذكير دوري"** ⏰
5. **ستبدأ الإشعارات فوراً!** 🎉

### ملاحظات مهمة:

- ✅ الإشعار الأول يظهر فوراً
- ⏰ ثم كل 3 دقائق بعد ذلك
- 🔕 يتوقف تلقائياً عند كتم الإشعارات
- 💾 يحفظ الحالة عند إغلاق التطبيق
- 🔄 يستعيد الحالة عند إعادة الفتح

---

## 🔧 التخصيص:

### تغيير المدة الزمنية:

في `notification_service.dart`:

```dart
// تغيير من 3 دقائق إلى 5 دقائق مثلاً
static const int _notificationIntervalMinutes = 5;
```

### إضافة رسائل جديدة:

في دالة `_showPeriodicNotification()`:

```dart
final messages = [
  '💪 حان وقت تسجيل وجبتك!',
  '🍎 لا تنسى تناول وجبة صحية',
  // أضف رسائلك هنا
  '🌟 رسالتك الجديدة!',
];
```

---

## 📱 التجربة:

### السيناريو 1: تفعيل الإشعارات
```
1. المستخدم يفعّل "تذكير دوري"
2. يظهر إشعار فوراً: "💪 حان وقت تسجيل وجبتك!"
3. بعد 3 دقائق: "🍎 لا تنسى تناول وجبة صحية"
4. بعد 3 دقائق: "⏰ تذكير: سجل وجبتك الآن"
5. وهكذا...
```

### السيناريو 2: كتم الإشعارات
```
1. المستخدم يكتم الإشعارات العامة
2. يتوقف "تذكير دوري" تلقائياً
3. يتعطل زر التبديل
4. لا تظهر أي إشعارات
```

### السيناريو 3: إعادة فتح التطبيق
```
1. المستخدم يغلق التطبيق (والإشعارات مفعلة)
2. يعيد فتح التطبيق
3. تستعيد الإشعارات الدورية تلقائياً
4. تستمر الإشعارات كل 3 دقائق
```

---

## 🎨 التصميم:

### الألوان:
- **مفعّل**: أخضر (Primary Color)
- **معطّل**: رمادي
- **الأيقونة**: ⏰ (منبه)

### الأنيميشن:
- انتقال سلس عند التبديل
- تغيير اللون تدريجياً
- مدة الأنيميشن: 200ms

---

## 🔔 أنواع الإشعارات:

### 1. إشعارات الوجبات
- تظهر عند إضافة وجبة
- تحتوي على اسم الوجبة والسعرات

### 2. إشعارات النصائح
- نصائح يومية
- معلومات غذائية

### 3. إشعارات دورية (جديد!)
- كل 3 دقائق
- رسائل تحفيزية متنوعة
- تذكير بتسجيل الوجبات

---

## ⚙️ الإعدادات المتقدمة:

### في SharedPreferences:

```dart
'periodicNotificationsEnabled' → true/false
'notificationsMuted' → true/false
```

### في NotificationService:

```dart
_periodicTimer → Timer instance
_notificationIntervalMinutes → 3
```

---

## 🐛 معالجة الأخطاء:

```dart
// إذا فشل عرض الإشعار
errorBuilder: (context, error, stackTrace) {
  debugPrint('Notification error: $error');
  return Container(); // لا يؤثر على التطبيق
}

// إذا كانت الإشعارات مكتومة
if (isMuted) {
  stopPeriodicNotifications();
  return;
}
```

---

## 📊 الإحصائيات:

- **عدد الإشعارات في الساعة**: 20 إشعار
- **عدد الإشعارات في اليوم**: 480 إشعار (إذا كان التطبيق مفتوح)
- **عدد الرسائل المختلفة**: 10 رسائل
- **الفاصل الزمني**: 3 دقائق (180 ثانية)

---

## ✅ الاختبار:

### اختبار يدوي:

1. ✅ تفعيل الإشعارات الدورية
2. ✅ التحقق من ظهور الإشعار فوراً
3. ✅ الانتظار 3 دقائق والتحقق من الإشعار الثاني
4. ✅ إيقاف الإشعارات والتحقق من التوقف
5. ✅ إغلاق وإعادة فتح التطبيق
6. ✅ التحقق من استعادة الحالة

---

## 🎉 النتيجة النهائية:

الآن المستخدمون سيحصلون على:
- ✅ تذكيرات منتظمة كل 3 دقائق
- ✅ رسائل تحفيزية متنوعة
- ✅ تحكم كامل من الإعدادات
- ✅ تجربة مستخدم سلسة
- ✅ حفظ واستعادة تلقائية

---

**تاريخ التحديث**: 2026-05-16  
**الحالة**: ✅ جاهز للاستخدام  
**الإصدار**: 1.0.0
