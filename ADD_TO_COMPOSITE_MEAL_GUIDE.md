# دليل إضافة وجبة لوجبة مركبة 🍽️

## الميزة الجديدة: إضافة وجبة لوجبة مركبة مباشرة! ✨

تم تطوير ميزة "إضافة" في قائمة خيارات الوجبة لتسمح بإضافة الوجبة لوجبة مركبة جديدة أو موجودة.

---

## 🎯 كيفية الاستخدام:

### الطريقة 1: إنشاء وجبة مركبة جديدة

```
1. اضغط على أيقونة ⋮ في كارت الوجبة
2. اختر "إضافة"
3. اضغط على "إنشاء وجبة مركبة جديدة"
4. ✅ تم! تم إنشاء وجبة مركبة جديدة مع الوجبة المحددة
```

### الطريقة 2: إضافة لوجبة مركبة موجودة

```
1. اضغط على أيقونة ⋮ في كارت الوجبة
2. اختر "إضافة"
3. اختر وجبة مركبة من القائمة
4. ✅ تم! تمت إضافة الوجبة للوجبة المركبة
```

---

## 🎨 التصميم:

### نافذة الاختيار (Bottom Sheet):

```
┌─────────────────────────────────────┐
│           ━━━━━                     │  ← Handle
├─────────────────────────────────────┤
│ ➕ إضافة لوجبة مركبة         ✕    │  ← Header
│    تفاح أحمر                        │
├─────────────────────────────────────┤
│                                     │
│  ┌───────────────────────────────┐  │
│  │ ➕ إنشاء وجبة مركبة جديدة   │  │  ← زر إنشاء
│  └───────────────────────────────┘  │
│                                     │
│  أو اختر وجبة موجودة               │
│                                     │
│  ┌───────────────────────────────┐  │
│  │ 🍽️ وجبة الإفطار              │  │
│  │    3 عناصر • 450 سعرة         │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │ 🍽️ وجبة الغداء               │  │
│  │    5 عناصر • 680 سعرة         │  │
│  └───────────────────────────────┘  │
│                                     │
└─────────────────────────────────────┘
```

---

## 📋 المكونات:

### 1. زر إنشاء وجبة مركبة جديدة

**التصميم**:
- خلفية متدرجة (Gradient) بالألوان الأساسية
- أيقونة ➕ بيضاء
- نص "إنشاء وجبة مركبة جديدة"
- ظل خفيف

**الوظيفة**:
```dart
- ينشئ وجبة مركبة جديدة
- يضيف الوجبة المحددة كأول عنصر
- يحفظ في قاعدة البيانات
- يعرض رسالة نجاح مع زر "عرض"
```

---

### 2. قائمة الوجبات المركبة الموجودة

**التصميم**:
- كارت لكل وجبة مركبة
- أيقونة 🍽️
- اسم الوجبة
- عدد العناصر والسعرات
- سهم للإشارة للإجراء

**الوظيفة**:
```dart
- يعرض جميع الوجبات المركبة
- مرتبة حسب التاريخ (الأحدث أولاً)
- عند الضغط: يضيف الوجبة للوجبة المركبة
- يحدث قاعدة البيانات
- يعرض رسالة نجاح
```

---

### 3. حالة فارغة

**عند عدم وجود وجبات مركبة**:
```
┌─────────────────────────────────────┐
│                                     │
│           🍽️                        │
│                                     │
│      لا توجد وجبات مركبة            │
│                                     │
└─────────────────────────────────────┘
```

---

## 🔧 الكود المضاف:

### 1. دالة `_addToCompositeMeal`:

```dart
void _addToCompositeMeal(MealModel meal) {
  showModalBottomSheet(
    // عرض نافذة الاختيار
    // زر إنشاء جديد
    // قائمة الوجبات الموجودة
  );
}
```

### 2. دالة `_buildCompositeMealOption`:

```dart
Widget _buildCompositeMealOption(
  BuildContext ctx,
  CompositeMealModel compositeMeal,
  MealModel mealToAdd,
  bool isDark,
) {
  // بناء كارت الوجبة المركبة
}
```

### 3. دالة `_createNewCompositeMeal`:

```dart
void _createNewCompositeMeal(MealModel meal) {
  // إنشاء وجبة مركبة جديدة
  final newCompositeMeal = CompositeMealModel(
    id: DateTime.now().millisecondsSinceEpoch.toString(),
    name: 'وجبة مركبة جديدة',
    items: [/* الوجبة المحددة */],
    mealType: meal.mealType,
    createdAt: DateTime.now(),
  );
  
  CompositeMealService.addCompositeMeal(newCompositeMeal);
}
```

### 4. دالة `_addMealToExistingComposite`:

```dart
void _addMealToExistingComposite(
  CompositeMealModel compositeMeal,
  MealModel mealToAdd,
) {
  // إضافة الوجبة للوجبة المركبة الموجودة
  final updatedItems = [...compositeMeal.items, newItem];
  
  final updatedMeal = CompositeMealModel(
    id: compositeMeal.id,
    name: compositeMeal.name,
    items: updatedItems,
    // ...
  );
  
  CompositeMealService.updateCompositeMeal(updatedMeal);
}
```

---

## 📱 السيناريوهات:

### السيناريو 1: إنشاء وجبة مركبة جديدة

```
1. المستخدم يضغط على ⋮ في كارت "تفاح أحمر"
2. يختار "إضافة"
3. تظهر نافذة الاختيار
4. يضغط على "إنشاء وجبة مركبة جديدة"
5. يتم إنشاء وجبة مركبة جديدة باسم "وجبة مركبة جديدة"
6. تحتوي على "تفاح أحمر" كأول عنصر
7. تظهر رسالة: "تم إنشاء وجبة مركبة جديدة مع تفاح أحمر"
8. يظهر زر "عرض" للانتقال لصفحة الوجبات المركبة
```

### السيناريو 2: إضافة لوجبة موجودة

```
1. المستخدم يضغط على ⋮ في كارت "موز"
2. يختار "إضافة"
3. تظهر نافذة الاختيار
4. يرى قائمة الوجبات المركبة:
   - وجبة الإفطار (3 عناصر • 450 سعرة)
   - وجبة الغداء (5 عناصر • 680 سعرة)
5. يختار "وجبة الإفطار"
6. يتم إضافة "موز" لوجبة الإفطار
7. تصبح وجبة الإفطار (4 عناصر • 539 سعرة)
8. تظهر رسالة: "تمت إضافة موز إلى وجبة الإفطار"
9. يظهر زر "عرض" للانتقال لصفحة الوجبات المركبة
```

### السيناريو 3: لا توجد وجبات مركبة

```
1. المستخدم يضغط على ⋮ في كارت "برتقال"
2. يختار "إضافة"
3. تظهر نافذة الاختيار
4. يرى زر "إنشاء وجبة مركبة جديدة"
5. يرى رسالة "لا توجد وجبات مركبة" مع أيقونة 🍽️
6. يضغط على زر الإنشاء
7. يتم إنشاء وجبة مركبة جديدة مع "برتقال"
```

---

## ✨ المميزات:

### 1. سهولة الاستخدام
- ✅ خطوات بسيطة وواضحة
- ✅ خياران: جديد أو موجود
- ✅ رسائل تأكيد واضحة

### 2. التصميم
- ✅ نافذة منبثقة جميلة
- ✅ زر إنشاء بارز ومميز
- ✅ قائمة منظمة للوجبات
- ✅ دعم الوضع الليلي

### 3. المرونة
- ✅ إنشاء وجبة جديدة
- ✅ إضافة لوجبة موجودة
- ✅ عرض معلومات الوجبة
- ✅ انتقال سريع للوجبات المركبة

### 4. التفاعل
- ✅ أنيميشن سلس
- ✅ استجابة فورية
- ✅ رسائل نجاح مع أزرار إجراء

---

## 🎨 الألوان والتصميم:

### زر الإنشاء:
- **الخلفية**: Gradient (Primary → Primary Dark)
- **النص**: أبيض
- **الأيقونة**: ➕ بيضاء
- **الظل**: أخضر خفيف

### كارت الوجبة المركبة:
- **الخلفية**: رمادي فاتح (Light) / رمادي غامق (Dark)
- **الحدود**: رمادي
- **الأيقونة**: 🍽️ مع خلفية خضراء فاتحة
- **النص**: أسود (Light) / أبيض (Dark)

---

## 📊 البيانات المحفوظة:

### عند إنشاء وجبة مركبة جديدة:

```dart
CompositeMealModel {
  id: "1715875200000",
  name: "وجبة مركبة جديدة",
  items: [
    CompositeMealItem {
      foodName: "تفاح أحمر",
      calories: "52",
      protein: "0.3",
      carbs: "14",
      fat: "0.2",
      categoryName: "فواكه",
      categoryEmoji: "🍎",
      categoryColorValue: 0xFFF44336,
      quantity: 1.0,
    }
  ],
  mealType: "breakfast",
  createdAt: DateTime.now(),
  notes: null,
}
```

### عند الإضافة لوجبة موجودة:

```dart
// يتم تحديث قائمة items فقط
updatedItems = [...existingItems, newItem]
```

---

## 🔄 التكامل مع الوجبات المركبة:

### الانتقال لصفحة الوجبات المركبة:

```dart
Navigator.pushNamed(context, '/composite_meals');
```

### عرض الوجبة المركبة:

- يمكن عرض الوجبة المركبة في صفحة الوجبات المركبة
- يمكن تعديلها أو حذفها
- يمكن إضافة المزيد من العناصر

---

## 🎯 الفوائد:

### للمستخدم:
- ✅ **سرعة**: إضافة وجبة بضغطة واحدة
- ✅ **مرونة**: اختيار بين جديد أو موجود
- ✅ **وضوح**: معلومات واضحة عن كل وجبة
- ✅ **سهولة**: واجهة بسيطة ومباشرة

### للتطبيق:
- ✅ **تنظيم**: تجميع الوجبات المتشابهة
- ✅ **تتبع**: حساب القيم الغذائية الإجمالية
- ✅ **إدارة**: سهولة إدارة الوجبات المركبة
- ✅ **تحليل**: بيانات أفضل للتحليل

---

## 🚀 التطويرات المستقبلية:

### 1. تخصيص الكمية
- [ ] تحديد كمية الوجبة عند الإضافة
- [ ] مضاعفة الوجبة (×2، ×3)
- [ ] كسور (½، ¼)

### 2. تسمية ذكية
- [ ] اقتراح اسم للوجبة المركبة
- [ ] بناءً على المحتويات
- [ ] بناءً على الوقت

### 3. فلترة وبحث
- [ ] فلترة الوجبات المركبة حسب النوع
- [ ] بحث في الوجبات المركبة
- [ ] ترتيب حسب السعرات

### 4. معاينة سريعة
- [ ] عرض محتويات الوجبة المركبة
- [ ] قبل الإضافة
- [ ] مع القيم الغذائية

---

## ✅ الاختبار:

### اختبار يدوي:

1. ✅ إنشاء وجبة مركبة جديدة
2. ✅ إضافة لوجبة موجودة
3. ✅ عرض قائمة الوجبات المركبة
4. ✅ حالة فارغة (لا توجد وجبات)
5. ✅ رسائل النجاح
6. ✅ الانتقال لصفحة الوجبات المركبة
7. ✅ الوضع الليلي

---

## 🎉 النتيجة النهائية:

الآن المستخدمون يمكنهم:
- ✅ إضافة وجبة لوجبة مركبة بسهولة
- ✅ إنشاء وجبات مركبة جديدة بسرعة
- ✅ إضافة لوجبات مركبة موجودة
- ✅ عرض معلومات الوجبات المركبة
- ✅ الانتقال السريع للوجبات المركبة
- ✅ تجربة مستخدم محسّنة

---

**تاريخ التحديث**: 2026-05-16  
**الحالة**: ✅ جاهز للاستخدام  
**الإصدار**: 1.0.0
