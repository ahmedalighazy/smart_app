# دليل قائمة خيارات الوجبة 📋

## الميزة الجديدة: قائمة منبثقة عند الضغط المطول! 🎯

تم إضافة قائمة خيارات تظهر عند الضغط المطول على أي وجبة في صفحة "وجباتي".

---

## 🎨 كيفية الاستخدام:

### للمستخدم:

1. **افتح صفحة "وجباتي"** 🍽️
2. **اضغط مطولاً** (Long Press) على أي وجبة
3. **ستظهر قائمة منبثقة** من الأسفل
4. **اختر الإجراء المطلوب**:
   - ➕ **إضافة** - إضافة الوجبة لوجبة مركبة
   - ✏️ **تعديل** - تعديل بيانات الوجبة
   - 🗑️ **حذف** - حذف الوجبة نهائياً

---

## 📋 الخيارات المتاحة:

### 1. ➕ إضافة
**الوظيفة**: إضافة الوجبة لوجبة مركبة

**التفاصيل**:
- اللون: أخضر (Primary)
- الأيقونة: `add_circle_outline`
- العنوان: "إضافة"
- الوصف: "إضافة لوجبة مركبة"

**الإجراء**:
```dart
- يعرض رسالة تأكيد
- يوفر زر "فتح" للانتقال لصفحة الوجبات المركبة
```

---

### 2. ✏️ تعديل
**الوظيفة**: تعديل بيانات الوجبة

**التفاصيل**:
- اللون: أزرق
- الأيقونة: `edit_outlined`
- العنوان: "تعديل"
- الوصف: "تعديل بيانات الوجبة"

**الإجراء**:
```dart
- يعرض رسالة تأكيد
- (يمكن إضافة نافذة تعديل لاحقاً)
```

---

### 3. 🗑️ حذف
**الوظيفة**: حذف الوجبة نهائياً

**التفاصيل**:
- اللون: أحمر (Error)
- الأيقونة: `delete_outline`
- العنوان: "حذف"
- الوصف: "حذف الوجبة نهائياً"

**الإجراء**:
```dart
- يعرض نافذة تأكيد
- يطلب تأكيد الحذف
- يحذف الوجبة من قاعدة البيانات
- يعرض رسالة نجاح
```

---

## 🎨 التصميم:

### القائمة المنبثقة (Bottom Sheet):

```
┌─────────────────────────────┐
│         ━━━━━               │  ← Handle
├─────────────────────────────┤
│ 🍎 تفاح أحمر               │  ← Header
├─────────────────────────────┤
│ ➕ إضافة                    │
│    إضافة لوجبة مركبة        │
├─────────────────────────────┤
│ ✏️ تعديل                    │
│    تعديل بيانات الوجبة      │
├─────────────────────────────┤
│ 🗑️ حذف                      │
│    حذف الوجبة نهائياً       │
└─────────────────────────────┘
```

### المميزات:
- ✅ تصميم نظيف وواضح
- ✅ أيقونات ملونة لكل خيار
- ✅ عنوان ووصف لكل خيار
- ✅ سهم للإشارة للإجراء
- ✅ فواصل بين الخيارات
- ✅ دعم الوضع الليلي

---

## 🔧 الكود المضاف:

### 1. تعديل `_buildMealCard`:

```dart
child: GestureDetector(
  onLongPress: () => _showMealOptionsMenu(context, meal, isDark),
  child: Container(
    // ... محتوى الكارت
  ),
),
```

### 2. دالة `_showMealOptionsMenu`:

```dart
void _showMealOptionsMenu(
  BuildContext context,
  MealModel meal,
  bool isDark,
) {
  showModalBottomSheet(
    context: context,
    backgroundColor: Colors.transparent,
    builder: (ctx) => Container(
      // ... محتوى القائمة
    ),
  );
}
```

### 3. دالة `_buildMenuOption`:

```dart
Widget _buildMenuOption({
  required BuildContext context,
  required IconData icon,
  required String title,
  required String subtitle,
  required Color color,
  required bool isDark,
  required VoidCallback onTap,
  bool isLast = false,
}) {
  // ... بناء عنصر القائمة
}
```

### 4. دوال الإجراءات:

```dart
void _addToCompositeMeal(MealModel meal) { }
void _editMeal(MealModel meal) { }
void _confirmDeleteMeal(MealModel meal) { }
```

---

## 📱 السيناريوهات:

### السيناريو 1: إضافة لوجبة مركبة

```
1. المستخدم يضغط مطولاً على وجبة "تفاح أحمر"
2. تظهر القائمة المنبثقة
3. يختار "إضافة"
4. تظهر رسالة: "سيتم إضافة تفاح أحمر لوجبة مركبة"
5. يظهر زر "فتح" للانتقال للوجبات المركبة
```

### السيناريو 2: تعديل وجبة

```
1. المستخدم يضغط مطولاً على وجبة "موز"
2. تظهر القائمة المنبثقة
3. يختار "تعديل"
4. تظهر رسالة: "تعديل موز"
5. (يمكن إضافة نافذة تعديل لاحقاً)
```

### السيناريو 3: حذف وجبة

```
1. المستخدم يضغط مطولاً على وجبة "برتقال"
2. تظهر القائمة المنبثقة
3. يختار "حذف"
4. تظهر نافذة تأكيد: "هل أنت متأكد من حذف برتقال؟"
5. يضغط "حذف"
6. تُحذف الوجبة من قاعدة البيانات
7. تظهر رسالة: "تم حذف برتقال"
```

---

## 🎯 المميزات:

### 1. سهولة الاستخدام
- ✅ ضغطة مطولة بسيطة
- ✅ قائمة واضحة ومنظمة
- ✅ خيارات محددة

### 2. التصميم
- ✅ ألوان مميزة لكل خيار
- ✅ أيقونات واضحة
- ✅ نصوص توضيحية
- ✅ دعم الوضع الليلي

### 3. الأمان
- ✅ تأكيد قبل الحذف
- ✅ رسائل واضحة
- ✅ إمكانية الإلغاء

### 4. التفاعل
- ✅ أنيميشن سلس
- ✅ استجابة فورية
- ✅ رسائل تأكيد

---

## 🔄 الطرق البديلة للحذف:

### 1. الضغط المطول (جديد!)
```
ضغط مطول → قائمة → حذف → تأكيد
```

### 2. السحب (Swipe) - موجود مسبقاً
```
سحب من اليمين لليسار → حذف مباشر
```

---

## 🎨 الألوان المستخدمة:

| الخيار | اللون | الكود |
|--------|-------|-------|
| إضافة | أخضر | `AppColors.primary` |
| تعديل | أزرق | `Colors.blue` |
| حذف | أحمر | `AppColors.error` |

---

## 📊 الإحصائيات:

- **عدد الخيارات**: 3
- **الوقت للفتح**: فوري
- **الأنيميشن**: 200ms
- **دعم الوضع الليلي**: ✅

---

## 🐛 معالجة الأخطاء:

```dart
// إذا فشل فتح القائمة
try {
  _showMealOptionsMenu(context, meal, isDark);
} catch (e) {
  debugPrint('Error showing menu: $e');
}

// إذا فشل الحذف
try {
  HiveService.deleteMeal(meal.id);
} catch (e) {
  ScaffoldMessenger.of(context).showSnackBar(
    SnackBar(content: Text('فشل الحذف')),
  );
}
```

---

## 🚀 التطويرات المستقبلية:

### 1. تعديل الوجبة
- [ ] إضافة نافذة تعديل كاملة
- [ ] تعديل الكمية
- [ ] تعديل نوع الوجبة
- [ ] تعديل التاريخ

### 2. إضافة لوجبة مركبة
- [ ] اختيار الوجبة المركبة مباشرة
- [ ] إنشاء وجبة مركبة جديدة
- [ ] تحديد الكمية

### 3. خيارات إضافية
- [ ] مشاركة الوجبة
- [ ] نسخ الوجبة
- [ ] إضافة للمفضلة
- [ ] عرض التفاصيل

---

## ✅ الاختبار:

### اختبار يدوي:

1. ✅ الضغط المطول يفتح القائمة
2. ✅ جميع الخيارات تعمل
3. ✅ التأكيد قبل الحذف
4. ✅ الرسائل تظهر بشكل صحيح
5. ✅ الوضع الليلي يعمل
6. ✅ الإغلاق بالضغط خارج القائمة

---

## 🎉 النتيجة النهائية:

الآن المستخدمون يمكنهم:
- ✅ الوصول السريع لخيارات الوجبة
- ✅ إضافة الوجبة لوجبة مركبة
- ✅ تعديل بيانات الوجبة
- ✅ حذف الوجبة بأمان
- ✅ تجربة مستخدم محسّنة

---

**تاريخ التحديث**: 2026-05-16  
**الحالة**: ✅ جاهز للاستخدام  
**الإصدار**: 1.0.0
